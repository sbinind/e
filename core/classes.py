class User:
    def __init__(self, id=None):
        if id is not None:
            pass
        else:
            self.__ID = self.ID = id
            self.username = username
            self.first_name = first_name
            self.last_name = last_name
